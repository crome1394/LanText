## What

## Why

## How to test

- [ ] `./gradlew :app:assembleDebug`
- [ ] SMS still sends through the default messenger
- [ ] If this touches pictures: MMS actually leaves the carrier, not only the local store
- [ ] Server still refuses to listen off the allowlisted SSID
